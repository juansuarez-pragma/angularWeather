import { Routes } from '@angular/router';
import { CurrentWeatherComponent } from './current-weather.component';

export const CURRENT_WEATHER_ROUTES: Routes = [
  {
    path: '',
    component: CurrentWeatherComponent
  }
];
